package com.halflife3.Networking.Client;

import javafx.stage.Stage;

public class ClientWindow extends ClientGame {
    private Stage window = null;
    public ClientWindow(Stage Window) {
        window = Window;
    }
}
